plugins {
    id 'java'
}

jar {
    manifest {
        attributes(
                'Main-Class': 'com.google.cloud.opentelemetry.example.metrics.MetricsExporterExample'
        )
    }
}

task fatJar(type: Jar) {
    manifest.from jar.manifest
    archiveClassifier = 'all'
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    } {
        exclude "META-INF/*.SF"
        exclude "META-INF/*.DSA"
        exclude "META-INF/*.RSA"
    }
    with jar
}

description = 'Examples for Cloud Monitoring Exporter'

dependencies {
    compile 'io.opentelemetry:opentelemetry-exporters-logging:0.9.1'
    compile(libraries.opentelemetry_api)
    compile(libraries.opentelemetry_sdk)
    compile(libraries.google_cloud_monitoring)
    compile project(':exporter-metrics')
}
